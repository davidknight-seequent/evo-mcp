# Evo MCP - Docker Environment Variables
# Copy to .env.docker and configure with your Evo credentials

# Required: Your Evo Platform Credentials
EVO_CLIENT_ID=daves-evo-client
EVO_REDIRECT_URL=http://localhost:32369/auth/callback

# Optional: Evo Platform URLs (defaults provided)
# EVO_DISCOVERY_URL=https://discover.api.seequent.com
# ISSUER_URL=https://ims.bentley.com

# MCP Transport Configuration
MCP_TRANSPORT=sse  # Options: stdio, sse, http
MCP_HOST=0.0.0.0
MCP_PORT=8000
# MCP_PATH=/mcp  # For http transport

# Optional: MCP Server Configuration
# MCP_TOOL_FILTER=all  # Options: all, admin, data
# EVO_MCP_LOG_LEVEL=ERROR  # Options: DEBUG, INFO, WARNING, ERROR

# Optional: Client Credentials Auth (for headless/CI/sandbox usage)
# Leave empty to use authorization_code flow (browser-based)
# EVO_AUTH_MODE=authorization_code  # or client_credentials
# EVO_CLIENT_SECRET=your-client-secret-here

# Optional: Local data directory for file-based operations
# EVO_LOCAL_DATA_DIR=./data
