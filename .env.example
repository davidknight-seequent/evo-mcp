# Fill in these two variables so that match your personal application
# registration.
# See the "Configure your Evo credentials" section of the README.
# They may be overridden by the MCP config set in your client.
EVO_CLIENT_ID=Replace this with your client ID
EVO_REDIRECT_URL=http://localhost:3000/signin-oidc

# MCP Server Configuration
MCP_TOOL_FILTER=all

# MCP Transport Configuration (Docker deployments)
# Default: stdio (for IDE integration)
# Options: stdio, sse, http
MCP_TRANSPORT=stdio
# MCP_HOST=0.0.0.0           # Bind address for sse/http transport
# MCP_PORT=8000              # Port for sse/http transport
# MCP_PATH=/mcp              # HTTP path (http transport only)

# MCP Logging Configuration
# Options: DEBUG, INFO, WARNING, ERROR
# EVO_MCP_LOG_LEVEL=ERROR

# Search path for local data files. Point this to local data you want tools to
# be able to access. Defaults to "data" in the repo root.
# EVO_LOCAL_DATA_DIR=/path/to/data

# Authority URLs for key systems, you shouldn't need to change these.
ISSUER_URL=https://ims.bentley.com
EVO_DISCOVERY_URL=https://discover.api.seequent.com

# Optional: Client Credentials Authentication (for headless/CI usage)
# Leave blank to use browser-based OAuth (default)
# EVO_AUTH_MODE=authorization_code  # or client_credentials
# EVO_CLIENT_SECRET=

# Example Agent Config
EVO_AGENT_MODEL=gemini-2.5-flash

# Example agent ADK config
GOOGLE_GENAI_USE_VERTEXAI=1
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=
